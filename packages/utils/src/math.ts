/**
 * @description: 加法
 * @param {number} a 第一个数
 * @param {number} b 第二个数
 * */
export function add(a: number, b: number) {
  return a + b;
}

/**
 * @description: 减法
 * @param {number} a 第个数
 * @param {number} b 第二个数
 * */
export function minus(a: number, b: number) {
  return a - b;
}
